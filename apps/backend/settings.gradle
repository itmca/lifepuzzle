rootProject.name = 'lifepuzzle-backend'

include 'shared:java-common'

// Only include projects that exist (for Docker builds)
if (file('services/lifepuzzle-api').isDirectory()) {
    include 'services:lifepuzzle-api'
}
